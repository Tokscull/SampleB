import { StatusCodeFillPipePipe } from './status-code-fill-pipe.pipe';

describe('StatusCodeFillPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusCodeFillPipePipe();
    expect(pipe).toBeTruthy();
  });
});
